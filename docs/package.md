# Package Layout

This repository follows the upstream `VGGT` package layout and keeps the JAX/Flax implementation under:

- `vggt/models`
- `vggt/heads`
- `vggt/layers`
- `vggt/utils`
- `vggt/dependency`

Main entrypoint:

- `vggt/models/vggt.py`
